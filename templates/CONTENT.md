<!--
  CONTENT.md — the locked content/messaging contract (parallel to DESIGN.md).
  Written/updated by the content-strategist. cila re-reads this every session; it is the
  source of truth for WHAT the site says and HOW it's phrased. Content hierarchy here drives
  the visual hierarchy in DESIGN.md. Replace every <…>. Ground copy in real specifics + VoC.
-->

# Content Contract — <Project>

## Positioning  (Dunford — 5 components)
- **Competitive alternatives:** <what they'd use/do otherwise — incl. "do nothing" / spreadsheet / manual>
- **Unique attributes (+ proof):** <what we have that alternatives don't — each with evidence>
- **Value themes (3–4):** <attributes clustered into the value the user cares about>
- **Who cares most (ICP):** <defined by job/pain/circumstance, NOT demographics>
- **Market category:** <the frame that makes the value obvious> · **Style:** <head-to-head | big-fish-small-pond | new-category>
- **Why now (optional):** <trend that adds urgency, only if it reinforces the category>

## The customer  (JTBD + VPC)
- **Job story:** When <situation>, I want to <motivation>, so I can <outcome>.
- **Ranked pains:** <top → nice-to-have>
- **Ranked gains:** <top → nice-to-have>
- **Forces:** push `<…>` · pull `<…>` · habit `<…>` · anxiety `<…>` (amplify push/pull; reduce anxiety; break habit)

## Core message & hierarchy
- **ONE core message (hero promise):** <the single most differentiated value, in the customer's words — passes "who else could say this?">
- **One-liner (BrandScript):** <Character> struggles with <Problem>; we provide <Plan> so they can <Success>.
- **Supporting pillars (3–4):** each = a value theme + unique attribute + proof → one page section.
  1. <pillar — attribute — proof>
  2. <…>
  3. <…>
- **Awareness stage (dominant visitor):** <unaware | problem | solution | product | most> → **page leads with:** <story/problem | problem | outcome | product+proof | offer>
- **Objections → reassurance:** <objection → how we defuse it (proof / guarantee / FAQ)>
- **Cut list:** <parity claims + low-ranked items demoted to table-stakes / dropped>

## Voice  (constant) & Tone (flexes by surface)
**We are __, __, __ — we are NOT __, __, __.**
| Trait | Do | Don't |
|---|---|---|
| <trait> | <…> | <…> |
- **Tone dimensions:** formal↔casual `<…>` · serious↔funny `<…>` · respectful↔irreverent `<…>` · matter-of-fact↔enthusiastic `<…>`
- **Vocabulary:** use `<signature terms>` · ban `<off-brand terms + the AI-slop kill-list>`
- (Personal project? load `~/Sync/vault/preferences/voice.md`.)

## Information architecture
- **Site type:** <marketing | SaaS | portfolio | docs> · **Global nav (5–7):** <…>
- **Pages:** <list> — each with its dominant **search intent** (<informational/navigational/commercial/transactional>).

## Page plan  (per page — the content model + handoff to the visual layer)
For `<page>` — section order (content-first; visual hierarchy must mirror this priority):
1. **Hero** — headline `<promise>` · subhead `<clarifying work>` · CTA `<verb+value>` — passes 5-second test
2. <section — job + framework (PAS/FAB/BAB) + the one point it makes + priority tier>
3. <…>
- **Primary CTA (one, repeated):** `<verb + value>` · friction microcopy: `<…>`

## SEO & accessibility
- `<title>` per page (unique, descriptive) · meta description (1–2 sentences) · one H1 + semantic headings · descriptive links/alt · JSON-LD: <Organization/Breadcrumb/Article/Product>. (No FAQPage rich-result targeting — removed 2026.)

## VoC swipe file
- Verbatim customer phrases (pains/gains/objections/triggers) to use in copy: `<…>`
